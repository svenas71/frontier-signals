---
layout: base.njk
title: Frontier Signals
---

<p class="text-sm opacity-60">
  posts: {{ collections.posts | length }}, links: {{ collections.links | length }}
</p>



<section class="space-y-12">
  <div class="prose prose-neutral dark:prose-invert">
    <h1 class="!mb-3">Frontier Signals</h1>
    <p class="lead text-xl">Calm analysis at the edge of life sciences, data, and venture. Essays + linked commentary by Sven Sewitz.</p>
  </div>

  <div>
  <h2 class="text-sm uppercase tracking-wide opacity-70 mb-3">Latest Essays</h2>
  <ul class="space-y-5">
    {% for post in collections.posts %}
      <li>
        <a href="{{ post.url }}" class="block">
          <div class="font-medium">{{ post.data.title }}</div>
          <div class="text-sm opacity-70">{{ post.date | readableDate }}</div>
        </a>
      </li>
    {% endfor %}
  </ul>
</div>

<div>
  <h2 class="text-sm uppercase tracking-wide opacity-70 mb-3">Latest Linked</h2>
  <ul class="space-y-5">
    {% for item in collections.links %}
      <li>
        <a href="{{ item.data.external_url }}" class="font-medium" rel="noopener nofollow">
          {{ item.data.title }}
        </a>
        {% if item.data.summary %}
          <div class="text-sm opacity-70">{{ item.data.summary }}</div>
        {% endif %}
      </li>
    {% endfor %}
  </ul>
</div>
</section>
